{
  "name": "validator-risk-api",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "ts-node-dev --respawn --transpile-only src/server.ts",
    "build": "tsc",
    "start": "node dist/server.js",
    "seed": "ts-node scripts/seed_samples.ts",
    "test": "jest --runInBand"
  },
  "dependencies": {
    "@clickhouse/client": "^2.0.0",
    "fastify": "^4.0.0",
    "@fastify/cors": "^8.0.0",
    "zod": "^3.0.0"
  },
  "devDependencies": {
    "ts-node": "^10.0.0",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.0.0",
    "jest": "^29.0.0",
    "ts-jest": "^29.0.0",
    "supertest": "^6.0.0",
    "@types/jest": "^29.0.0",
    "@types/supertest": "^2.0.0"
  }
}
